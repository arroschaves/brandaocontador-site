# Site institucional Brandão Contabilidade
# Copie para .env.local e preencha apenas se quiser envio por e-mail no formulário.

NODE_ENV=development

# Formulário de contato via SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
